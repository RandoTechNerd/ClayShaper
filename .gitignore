__pycache__/
*.pyc
build/
dist/
*.spec.bak

# Runtime / user data
custom_printers.json
wheel_profile.json
_upload.stl
sample_thumbs/

# NOTE: the bundled sample models are ClayShaper's own (tools/make_samples.py)
# plus a RandoTechNerd original, so they DO ship with the repo.

.DS_Store
Thumbs.db
